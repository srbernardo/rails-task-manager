<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <p>
      <input type="checkbox">
    <label>
      <%= link_to task.title, "tasks/#{task.id}" %>
    </label>
  </p>
<%end%>

<p>
  <%= link_to "Create a new task.", "tasks/new" %>
</p>
